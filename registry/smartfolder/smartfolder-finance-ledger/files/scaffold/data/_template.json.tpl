{
  "kind": "{{kind.name}}",
  "template_version": "{{kind.version}}",
  "data_schema_version": 2,
  "created_with": "{{kind.version}}",
  "migrated_at": "{{instance.created_at}}",
  "history": [
    { "to": "{{kind.version}}", "at": "{{instance.created_at}}", "migration": null, "by": "create" }
  ]
}
